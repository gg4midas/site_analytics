# site_analytics 最终版部署说明

本压缩包是整理后的**最终版**，可覆盖服务器 `/opt/site_analytics` 中对应的应用文件。

## 一、包内文件
| 路径 | 说明 | 是否必须覆盖 |
|------|------|------|
| `app.py` | 后端（Python http.server） | 是 |
| `index.html` | 看板前端 | 是 |
| `tracker.js` | 网页埋点脚本 | 是 |
| `static/`（含 echarts.min.js、world_map.json 等） | 前端依赖与世界地图 | 是 |
| `tracker-loader.html` | 部署片段参考（动态注入 loader） | 否，参考用 |
| `WPML单JS部署指南.md` | 多语言 / 缓存插件部署说明 | 否，参考用 |
| `update_geoip.sh` | 一键下载 / 更新 GeoLite2-City + GeoLite2-ASN 库（运营商识别依赖） | 否，需要运营商识别时用 |
| `start.sh` | **启动脚本**：`bash start.sh` 一键拉起服务（已内置 `--geoip-db`/`--asn-db`，后台 nohup 运行，日志 run.log） | 是，推荐用它启动 |
| `restart.sh` | **重启脚本**：`bash restart.sh` 先释放端口再以 `start.sh` 重新拉起 | 是，推荐用它重启 |

## 一（续）、服务器运行 / 重启命令（bash 脚本）

> 以下两条命令是本工具在服务器上的**标准启停方式**，已替你封装好（含运营商识别所需的 GeoIP/ASN 库参数、端口预检、后台运行、日志落盘）。部署后只需 `cd /opt/site_analytics` 再执行。

### ① 启动指令
```bash
cd /opt/site_analytics
bash start.sh                              # 默认端口 8899、无令牌
# 指定端口 + 访问令牌（推荐，二选一写法）：
PORT=8899 TOKEN=你的令牌 bash start.sh
# 或命令行参数写法：
bash start.sh --port 8899 --token 你的令牌
```
启动后输出 PID，日志写入 `run.log`；`start.sh` 已自动带 `--geoip-db geoip/GeoLite2-City.mmdb --asn-db geoip/GeoLite2-ASN.mmdb`（运营商识别依赖，缺库自动降级不报错）。

### ② 重启指令
```bash
cd /opt/site_analytics
bash restart.sh                           # 自动 fuser -k 释放端口，再用 start.sh 重新拉起
# 带参数：
bash restart.sh --port 8899 --token 你的令牌
```
> `restart.sh` = 释放端口（fuser/ lsof）+ 等待释放 + `bash start.sh`，无需手动 kill。

### ③ 验证 / 看日志
```bash
curl -I http://127.0.0.1:8899/            # 应返回 200
tail -f run.log                           # 实时日志（确认 "ASN: 已启用" 即运营商识别已加载）
ps aux | grep app.py                      # 确认进程在跑
```

## 二、服务器部署步骤（CentOS）
1. 上传覆盖：将 `app.py`、`index.html`、`tracker.js`、`static/` 上传到 `/opt/site_analytics/`（保持 `static/` 子目录结构）。
2. ⚠️ **切勿覆盖**数据文件：`web.db`（历史访问数据）、`geoip/`（MaxMind 库）、`run.log`。
   > 注意：本版的 `events.db` 才是事件库（旧版曾用 `web.db`）。覆盖时只替换应用文件，**保留现有 `events.db` / `geoip/`**，历史数据不会丢，且旧域名数据会自动归并到主域。
3. 重启后端：**推荐直接用封装脚本**（见上方「一（续）」节）：
   ```bash
   cd /opt/site_analytics
   bash restart.sh                 # 自动释放端口并重新拉起（含 GeoIP/ASN 参数）
   # 或首次启动：bash start.sh
   ```
   手动等效命令（仅当脚本不可用时的兜底写法，**务必先 `cd /opt/site_analytics`**，否则 `app.py` 与相对路径的 `--geoip-db` 找不到，进程会以退出码 2 崩溃）：
   ```bash
   cd /opt/site_analytics
   fuser -k 8899/tcp
   nohup python3 app.py --host 127.0.0.1 --port 8899 \
       --geoip-db geoip/GeoLite2-City.mmdb \
       --asn-db geoip/GeoLite2-ASN.mmdb > run.log 2>&1 &
   ```
   > `--asn-db` 用于识别访客运营商（ISP）。缺省即 `geoip/GeoLite2-ASN.mmdb`，若该库不存在会**自动降级**（运营商列显示为空，其余功能不受影响）。需要运营商识别时先执行 `bash update_geoip.sh` 下载库（需已在脚本内配置 MaxMind License Key，或使用其 db-ip 免费分支）。
   > ⚠️ 若重启后 `curl -I` 提示 `Connection refused` 且 `run.log` 里有 `can't open file 'app.py'`，就是没在 `/opt/site_analytics` 目录下执行——重新 `cd` 后再起即可。
4. 验证（在服务器上执行，确认进程与新代码已生效）：
   ```bash
   # 健康检查（现在应返回 200，而非 501）
   curl -I http://127.0.0.1:8899/
   # 确认是新代码（站点应归并为主域，如 example.com，而非 de/fr/www 各自列出）
   curl -s http://127.0.0.1:8899/api/sites | python3 -m json.tool
   # 确认数据在流（recent 窗口内应有记录）
   curl -s "http://127.0.0.1:8899/api/recent?site=example.com&window=60" | python3 -m json.tool
   ```
5. 看板验证：打开看板 → 右上角「站点管理」→ 复制「通用部署代码」（已自动用看板域名生成，无需手动替换）→ 粘贴到目标网站 `</head>` 前。

## 三、本版新增：运营商识别 + 异常流量标注 + 访客软屏蔽
- **访客运营商（ISP）**：基于 GeoLite2-ASN 库，服务端按访客 IP 解析运营商，展示在访客表 / 实时表的「运营商」列。内网 / 本地 IP 显示「(内网/本地)」；未装 ASN 库时该列为空、不报错。
- **异常流量标注（多条件，满足任一即标记）**：访客表对疑似爬虫访客自动打「疑似爬虫」标记，命中以下任一条件即标注：
  1. **单访客浏览量畸高**：该访客 PV ≥ 全部访客 PV 中位数 × 8，且 ≥ 30，且样本 ≥ 5 位访客；
  2. **数据中心 / 云主机 / 爬虫托管 ISP**：运营商命中已知 AS 号（如 AWS、Google、Azure、阿里云、腾讯云、Cloudflare、OVH、Vultr、Hetzner 等）或组织名关键词（amazon/google cloud/azure/hosting/datacenter/cdn/server/vps 等）。
  > 两类条件为「或」关系，覆盖「仅做数据抓取的托管流量」——即便 PV 不高，只要是数据中心来源也会标记。标注分为两种类型：命中数据中心 ISP 显示**紫色「疑似数据采集」**徽章，命中 PV 畸高显示**橙色「浏览量畸高」**徽章，两者并存显示「疑似爬虫/抓取」；访客页顶部提示区分数量（如 `⚠ 80 个疑似异常（80 个疑似数据采集）`）。标记原因写明具体命中项（如 `运营商「OCULUS NETWORKS INC」为数据中心/云主机网络（疑似数据采集/抓取）` 或 `单访客浏览量畸高`）。`/api/visitors` 额外返回 `suspect_dc_count` 与 `suspect_highpv_count`。
  > **识别库持续扩充**：已覆盖 AWS / Google / Azure / 阿里云 / 腾讯云 / 华为云 / Baidu / Oracle / Cloudflare / OVH / Vultr / Hetzner / Contabo / Linode / DigitalOcean / Fastly / Datacamp / M247 / OCULUS / Servers.com / Leaseweb / Ace / FDCservers 等（含对应 AS 号）；**刻意不**把 Starlink/SpaceX 等住宅卫星网络纳入，避免误伤真实用户。
- **确认后软屏蔽（仅屏蔽访客数据，可恢复）**：在访客 / 实时表点该访客的「剔除」按钮，即把该访客加入 `blocked_visitors`。屏蔽后所有统计（概览、趋势、访客、实时、性能等）通过数据库视图 `visible_events` 自动排除其数据，**但原始事件完整保留**，随时可在「站点管理 → 已屏蔽访客」中点「恢复」还原。
  > 设计要点：读统计走视图 `visible_events`，上报 / 删除 / 列站点等基表操作仍走 `events`，因此屏蔽不丢原始数据、可无损还原。
- **运营商（ISP）数据精准度（部署侧关键项）**：异常标注依赖 ISP 解析，须确保 **GeoLite2-ASN.mmdb 已部署且定期更新**。执行 `bash update_geoip.sh` 拉取 ASN 库（与 City 库一并），启动加 `--asn-db geoip/GeoLite2-ASN.mmdb`，日志确认 `ASN: 已启用`。若统计服务位于 Cloudflare / 反代之后，须透传真实访客 IP——代码已支持 `CF-Connecting-IP` / `X-Forwarded-For` / `X-Real-IP`（取链路首个），**切勿**把统计端点也藏在隐藏真实 IP 的代理后，否则全部流量会被误判为机房网络。ISP 无法解析时以空列展示，不臆造（不存储原始 IP，符合隐私合规）。
- **统计口径说明（与 Clicky / Google Analytics 有出入的原因）**：本工具不改动 UV / 会话语义，差异主要来自——① 不依赖第三方 JS，广告拦截 / 隐私插件对本工具基本无效（本工具计到、GA/Clicky 常漏计，故本工具偏高）；② Bot 过滤规则较 GA 宽松（现已加疑似爬虫标注 + 软屏蔽补齐）；③ 会话按 30 分钟滑动窗口切分；④ 时区按东八区聚合；⑤ 跳出 / UV 定义细节不同（UV=去重的有浏览量访客）。属正常口径差异，可用上面的标注 + 软屏蔽机制收敛异常流量。

## 四、本版相对上一版的主要变更
- **支持 HEAD 方法**：新增 `do_HEAD`，`curl -I`、uptime 监控、健康检查探针现在返回标准 `200` / `404`（仅响应头、无正文，Content-Length 与 GET 一致），不再出现 `501 Unsupported method ('HEAD')`。
- **主域归并 + 子域区分**：看板站点下拉框只显示主域（如 `example.com`、`example.org`），不再把 `www/de/fr` 等子域各自列成一项；选中主域后，统计自动聚合其下所有子域，并在「站点 / 语言分布」卡片（概览页）与访客表、实时表的「站点」列中显示各子域明细。
- **删除按主域级**：删除某个主域会连同其下所有子域数据一并清除。
- **数据上报诊断**：`tracker.js` 新增 `data-debug` 模式（见下方排查章节），可在浏览器控制台确认上报是否成功。
- 看板「站点管理」弹窗的通用部署代码：自动按看板域名生成动态注入 loader（无 `data-site`、带 `data-endpoint`），一段代码监控所有语言域名，并自动绕过 WP 缓存插件本地化，无需逐站配 exclude。
- 访客表移除冗余的「语言」列（多语言已由站点域名区分）。
- **内容页 URL 显示修复**：内容页（全部热门页面）表格的「页面 URL」列现在显示**完整路径**（如 `https://example.net/blog/2026/hello`），不再只显示 `www.+主域`；已移除「一级目录」「二级目录」分列，按标准页面 URL 一列展示。
- **移除自然搜索词 Top 模块**：因浏览器隐私策略（no-referrer / 搜索引擎隐藏搜索词）导致该模块基本无数据，已从概览页与后端聚合中移除，避免误导。
- **Core Web Vitals 汇总改为 75 分位并过滤异常值**：顶部汇总卡（FCP / LCP / TTFB / CLS / Speed Index）从「区间均值」改为 **p75（75 分位）**，与 PageSpeed Insights 字段数据口径对齐，更抗极端值；同时聚合时过滤大于 60 秒的时间指标及大于 2 的 CLS，并自动把疑似按毫秒上报的数值转换为秒。下方页面表格仍保留各页面均值。
- **自动刷新机制优化**：实时/访客/仪表盘自动刷新间隔分别调整为 15 秒 / 30 秒 / 60 秒，降低刷新打扰；顶部 tab 行右侧统一放置「刷新」按钮与「自动刷新」状态指示灯，全站唯一入口；展开访客操作记录行或打开站点管理弹窗时，自动暂停自动刷新，关闭/收起后恢复；移除访客页内重复的「自动刷新中」提示。
- **内容页热门页面统计修复**：修复 `get_stats` 中 `pages` 变量被性能分设备汇总意外覆盖的 bug，内容页「全部热门页面」表格现在正确按 `(site, path)` 聚合，显示完整 URL（如 `https://www.example.net/news/...`）与正确访问次数，不再全部显示主域且次数为 0。
- **地域展示中文本地化**：国家 / 地区列表与地域树中的国家名只显示中文，不再显示国旗 emoji（避免部分系统把国旗渲染为两位英文缩写）；城市名补充常见城市中英对照表（如 New York→纽约、Los Angeles→洛杉矶、London→伦敦、Singapore→新加坡等），未命中时保持原英文。
- **「疑似数据采集」独立标注维度**：在原「单访客浏览量畸高」之外，新增 `datacenter` 类型——命中云主机/数据中心/托管 ISP（AWS、阿里云、腾讯云、Fastly、Datacamp、M247、OCULUS、Cloudflare 等，已扩充并排除 Starlink 住宅卫星）即标，与 PV 无关。前端以**紫色「疑似数据采集」**徽章区分于橙色「浏览量畸高」；访客页顶部提示分别显示「疑似数据采集 / 浏览量畸高」数量，`/api/visitors` 返回 `suspect_dc_count` 与 `suspect_highpv_count`。针对「分布式、单 IP 低频」的数据采集流量（每 IP 仅 1–3 次、但机房 IP 众多）尤其有效。
- **运营商精准度部署说明**：文档新增「运营商数据精准度」章节，明确 `update_geoip.sh` 拉取 ASN 库、`--asn-db` 启用、以及反代透传真实访客 IP（`CF-Connecting-IP`/`X-Forwarded-For`/`X-Real-IP`）的要求；未部署 ASN 时 ISP 留空、异常标注覆盖率下降。

## 四（续）、本轮界面与统计优化（最新）

- **URL 查询串自动剥离（统计 + 展示）**：入库时即剔除 `?` 之后的查询参数与 `#` 之后片段（如 `?srsltid=AfmBOop...`、`?utm_source=...`），并在所有按路径聚合的查询（热门页面 / 落地页 / 退出页 / 停留时长）中统一归一化，使同一页面的不同参数变体归并统计；前端链接同样不再带查询串。例如 `/news/.../?srsltid=AfmBOopGz...` 归并为 `/news/.../`。
- **首页落地页 / 退出页 / 停留时长 TOP 修复 www 与多语言前缀**：这些表格此前统一用「当前选中站点」拼 URL，导致丢失 `www.` 或 `de./fr.` 等子域标识；现已与内容页一致，按每条记录真实的子域（`site` 字段）拼出完整链接。
- **首页热门页面 TOP 与内容页统一**：首页「热门页面 TOP」改用与内容页「全部热门页面」相同的渲染（带 `[www]`/`[fr]` 站点标签 + 完整超链接 + 访问量条），不再只是普通排名表。
- **默认浅色模式**：页面默认加载为浅色主题（移除默认深色）；右上角「浅色 / 深色」按钮仍可手动切换，切换后记忆选择。
- **访客会话表格去掉「站点」列**：该列与顶部站点选择器重复，已移除；操作详情中的逐页链接仍按各自子域正确显示。
- **首页图表两行重排**：第一行 = 访问趋势 + 访问深度分布；第二行 = 站点/语言分布 + 新访客/回访客 + 设备分布（三列等宽）；落地页/退出页、停留时长、热门页面维持下方。

## 四（续2）、本轮优化（地区中文化 + 潜在目标客户标注 + 机房流量自动排除）

- **访客页 / 实时监控页「地区」列彻底中文化（去掉英文缩写）**：此前国家 / 地区排行已只显示中文（且不显示国旗 emoji，避免部分系统把国旗渲染成两位英文缩写）；本轮对访客表与实时监控表的「地区」列同样改为只显示中文国家 / 地区名（沿用内置 `cnName` 映射），不再显示国旗 emoji 与英文国家名，全站地域展示口径统一。
- **「询盘」标注改为「潜在目标客户」**：访客表中疑似询盘客户的徽章文本由「询盘」改为「潜在目标客户」，更准确且不暗示已发生询盘；同步更新顶部筛选卡片标题、副标题说明与「仅看」筛选标签文案。
- **机房 / 数据中心流量自动排除，访客页「剔除」操作移除**：确认属于机房 / 数据中心网络（云主机、托管、CDN 等，依据 ASN 号与运营商组织名关键词识别）的访客，在采集时即自动加入排除名单（`blocked_visitors`），经 `visible_events` 视图统一排除，**不计入任何统计**；新增 `auto_block_datacenter_visitors()` 在启动时对历史数据回填，使已存在的机房流量同样被排除。因机房流量已自动排除，访客表与实时监控表的「剔除」按钮及相关确认逻辑已移除；设置页「已屏蔽访客」改为说明机房流量自动排除，并保留「恢复」入口以便误判时重新计入。

## 四（续3）、本轮优化（访问链接彻底只统计静态路径）

- **历史数据一次性归一化（启动自动清洗）**：此前查询串剥离只在「新数据入库」与「聚合查询」两处生效；若部署前库里已存带 `?`/`#` 的历史路径，访客会话的「访问过的页面」列表与实时监控的「当前路径」仍会原样显示查询串。新增 `clean_stored_paths()`，服务启动时对历史事件中仍含 `?` 查询串或 `#` 片段的路径执行一次性 `UPDATE` 归一化为静态路径（如 `/news/.../?srsltid=...` → `/news/.../`），幂等、无害、仅处理未清洗旧数据；启动日志会打印「历史路径清洗：已归一化 N 条」。
- **访客会话 / 实时监控路径查询时兜底清洗**：`get_visitors`（访客页「访问过的页面」列表）与 `get_recent`（实时监控「当前路径」及页面集合）现均在读取原始 `path` 时调用 `_clean_path()`，确保即便数据尚未迁移也绝不会显示查询串；与聚合查询的 `_CLEAN_PATH_SQL`、入库时的 `_clean_path` 共同构成「入库 + 聚合 + 展示 + 历史迁移」四重保障，访问链接统计在任何入口都只认静态路径。

## 四（续4）、本轮修复（概览 / 访客 / 来源三处独立访客数对齐）

用户反馈：概览标签的「独立访客」、访客标签的「访客统计数量」、来源页面的「各来源访客数汇总」三者对不上。根因与修复如下：

- **访客标签数量不再截断**：原 `get_visitors` 仅从「最近 3000 条事件」构建访客桶、并封顶显示 200 位，导致统计数量远小于真实值。现新增 `total_visitors`——对当前周期（含全部子域）执行 `COUNT(DISTINCT visitor)` 全量统计，不受 3000 事件截断与 200 分页影响；前端「访客统计数量」改用该值，与概览「独立访客」完全一致。列表仍按最近活跃取前 200 位展示，但统计数量用真实总数。
- **来源页改为「独立访客」口径且各来源之和 = 总独立访客数**：原来源类型 / 来源域名统计用的是 `COUNT(*)`（访问量），既非独立访客、求和也不等于总访客。现以**每位访客的「落地来源」**（窗口内首条 pageview 的 Referrer）为唯一归属进行独立访客计数（新增 `_landing_refs_sql`，不依赖 window 函数，兼容旧版 SQLite）。由于每位访客仅有唯一落地来源，各来源独立访客数之和**精确等于**概览总独立访客数，不再重复计数。来源类型明细表与来源域名 TOP 表表头由「次数」改为「独立访客」，并补充说明。
- **询盘访客数（潜在目标客户）全量准确**：`inquiry_count` 原基于被截断的访客桶，现改为对全周期执行与 `_is_potential_inquiry` 口径一致的 SQL 统计（URL 解码后子串匹配，含中日韩），不受截断影响。
- **接口健壮性**：`get_visitors` 空站点兜底由返回列表改为返回与正常一致的字典结构；`/api/visitors` 响应新增 `total_visitors` 字段并透传至前端；/api/visitors 空站点不再 500。

验证：合成数据下，概览 UV、来源类型独立访客之和、访客标签 total_visitors 在 7/28/90/365 天各范围均相等；来源子项（搜索引擎/AI 工具/社交媒体/外部链接/直接访问）按落地来源正确归类。

## 四（续5）、本轮新增（概览环比对比）

### 1. 概览页「环比对比（本期 vs 上一周期）」
- 概览页 6 张 KPI 卡（独立访客 / 浏览量 / 人均页面数 / 平均停留 / 跳出率 / 当前在线）现每张显示**上一等长周期的同比变化%**：升用绿色▲、降用红色▼（跳出率相反——上升为红、下降为绿）。卡片下方小字标注「上期」绝对值，方便对照。
- 趋势图叠加**上一周期虚线**（浏览量(上期) / 访客(上期)），与本期实线同一坐标轴对齐，直观对比走势。
- 口径：上一周期 = 与当前选定范围**等长、紧邻在前**的窗口（今天↔昨天、近7天↔前7天、近30天↔前30天、自定义区间↔其前的等长区间），均按东八区聚合；统计走 `get_stats` 新增的 `prev` 字段（含 pv/uv/会话数/跳出率/平均停留/人均页数与逐桶 `daily_prev`），与现有总量同源计算，不存在重复计数。

## 五、如何拿到监控代码（最终用法）
打开看板 → 右上角「站点管理」→ 复制弹窗顶部的「通用部署代码」→ 粘贴到目标网站 `</head>` 前（WP 主题 header 或 `wp_head` 钩子）。多语言站点同一段代码即可，无需为 www / de / fr 分别部署。

## 六、数据只到某天、之后没有新数据？（排查步骤）
若看板显示的数据停留在某日（如只有 7/9、7/10），说明之后 tracker 未成功上报。按序排查：

1. **确认部署代码是「通用部署代码」且已生效**：在 WP 后台确认 `</head>` 前存在那段**动态注入 loader**（以 `<script>(function(){...` 开头，非旧的直接 `<script src=.../tracker.js>`）。旧的直接引用方式容易被缓存插件本地化而失效。
2. **清缓存**：更新后到 WP 缓存插件（WP Rocket / Autoptimize / W3TC 等）点「清空缓存」，避免浏览器/服务器返回旧的页面与脚本。
3. **用调试模式验证上报**：临时在被统计页面 URL 后不影响的情况下，于那段 loader 标签上加 `data-debug="true"`（或直接在浏览器控制台执行让 tracker 重新加载），打开浏览器控制台（F12 → Console），正常应看到：
   ```
   [tracker] site=de.example.com endpoint=https://你的分析域名/api/event
   [tracker] send -> https://你的分析域名/api/event {…}
   [tracker] POST status 200
   ```
   若看到 `POST status 200` 即上报成功；若 `fetch failed` 或被 CSP / 网络拦截，需检查分析域名是否可公网访问、是否有跨域 / 防火墙限制。
4. **确认分析服务在跑**：服务器上 `curl -I http://127.0.0.1:8899/` 应返回 200；`ps aux | grep app.py` 确认进程在运行。
5. 排除上述后仍有缺口，多为某段时间站点未产生真实访问，或当时 tracker 未部署——属正常现象，补部署并清缓存后新访问会实时进入看板。

> 多语言（WPML）部署要点：一套代码、省略 `data-site`，tracker 自动用 `location.hostname` 区分 www/de/fr，看板按主域归并展示。详见 `WPML单JS部署指南.md`。
